<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiApp1.ViewModels"
             x:Class="MauiApp1.Views.BuscarPsicologoPage"
             x:DataType="vm:BuscarPsicologoViewModel"
             Title="Buscar Psicólogo"
             BackgroundColor="White">

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="20,16">

        <Label Grid.Row="0"
               Text="{Binding MensagemErro}"
               Style="{StaticResource TextoErro}"
               IsVisible="{Binding MensagemErro, Converter={StaticResource StringNaoVazioConverter}}"
               Margin="0,0,0,10" />

        <Border Grid.Row="1" Style="{StaticResource CampoTexto}" Margin="0,0,0,10">
            <Entry Text="{Binding TextoBusca}" Placeholder="Buscar por nome, CRP ou e-mail" VerticalOptions="Center" />
        </Border>

        <Border Grid.Row="2"
                IsVisible="{Binding TemConvitePendente}"
                StrokeShape="RoundRectangle 14"
                BackgroundColor="#E4F0FF"
                Stroke="Transparent"
                Padding="16,12"
                Margin="0,0,0,10">
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding AbrirConviteCommand}" />
            </Border.GestureRecognizers>
            <Label Text="🔔  1 novo convite de vínculo — toque para ver" TextColor="{StaticResource AzulPrimario}" FontAttributes="Bold" FontSize="13" />
        </Border>

        <RefreshView Grid.Row="3" Command="{Binding CarregarCommand}" IsRefreshing="{Binding Carregando}">
            <CollectionView ItemsSource="{Binding Psicologos}">
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="20">
                        <Label Text="Nenhum psicólogo encontrado." TextColor="{StaticResource CinzaMedio}" HorizontalTextAlignment="Center" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ItemPsicologoBusca">
                        <Border Style="{StaticResource CampoTexto}" Margin="0,6" HeightRequest="68">
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0" VerticalOptions="Center" Spacing="2">
                                    <Label Text="{Binding Nome}" FontAttributes="Bold" TextColor="{StaticResource AzulEscuro}" />
                                    <Label Text="{Binding Crp, StringFormat='CRP {0}'}" FontSize="12" TextColor="{StaticResource CinzaMedio}" />
                                </VerticalStackLayout>

                                <Button Grid.Column="1"
                                        Text="{Binding TextoBotao}"
                                        IsEnabled="{Binding PodeSolicitar}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SolicitarCommand}"
                                        CommandParameter="{Binding .}"
                                        Style="{StaticResource BotaoPrimario}"
                                        FontSize="12"
                                        HeightRequest="38"
                                        VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
